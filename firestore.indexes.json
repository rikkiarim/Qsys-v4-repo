{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": [
      {
        "collectionGroup": "list",
        "queryScope": "COLLECTION",
        "fields": [
          {
            "fieldPath": "timestamp",
            "order": "ASCENDING"
          }
        ]
      }
    ]
  }
}
